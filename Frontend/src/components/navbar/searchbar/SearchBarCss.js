export default function SearchBarCss() {
    return (`
        <style>
            .search-bar {
                background-color: black;
                margin: 10px;
            }

            .search-button {
                color: white;
                background-color: red;
                hover: black;
            }
        </style>`
    );
}