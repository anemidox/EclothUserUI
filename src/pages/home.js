import { Carousel } from "../components/home/Carousel.js";

const home = () => {
    return (`
        <div class="home">
            <app-carousel></app-carousel>
        </div>
    `);
}

export default home;